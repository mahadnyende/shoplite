import 'package:flutter/material.dart';
// TODO: Import your feature screens here as you implement them

class ShopLiteAppShell extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'ShopLite',
      // TODO: Set up routes, theme, and layout shell
      home: Scaffold(
        body: Center(child: Text('ShopLite App Shell')), // Placeholder
      ),
    );
  }
}
